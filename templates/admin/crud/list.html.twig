{% extends '@Forumify/admin/base.html.twig' %}
{% set title = (translationPrefix ~ 'plural')|trans|title %}
{% block title_page %}
    {{ title }}
{% endblock %}
{% block body %}
    <h2>
        {{ title }}
        <a class="btn-primary btn-small btn-icon" href="{{ path(route ~ '_create') }}">
            <i class="ph ph-plus"></i>
        </a>
    </h2>
    {{ component(table) }}
{% endblock %}
