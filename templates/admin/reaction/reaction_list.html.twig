{% extends '@Forumify/admin/base.html.twig' %}
{% block title_page %}
    {{ 'reactions'|trans }}
{% endblock %}
{% block body %}
    <h2 class="mb-4">
        {{ 'reactions'|trans }}
        <a class="btn-primary btn-small btn-icon" href="{{ path('forumify_admin_reaction_create') }}">
            <i class="ph ph-plus"></i>
        </a>
    </h2>
    {{ component('ReactionTable') }}
{% endblock %}
