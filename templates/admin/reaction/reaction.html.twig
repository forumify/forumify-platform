{% extends '@Forumify/admin/base.html.twig' %}
{% block title_page %}
    {% if reaction is not null %}
        Reaction {{ reaction.name }}
    {% else %}
        {{ 'create_reaction'|trans }}
    {% endif %}
{% endblock %}
{% block body %}
    {% include '@Forumify/admin/components/simple_form.html.twig' with {
        title: reaction is not null ? reaction.name : 'create_reaction'|trans,
        form: form,
        cancelPath: path('forumify_admin_reaction_list')
    } only %}
{% endblock %}
