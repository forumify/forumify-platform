{% extends '@Forumify/admin/base.html.twig' %}
{% block title_page %}
    {{ 'roles'|trans }}
{% endblock %}
{% block body %}
    <h2 class="mb-4">
        {{ 'roles'|trans }}
        <a class="btn-primary btn-small btn-icon" href="{{ path('forumify_admin_role_create') }}">
            <i class="ph ph-plus"></i>
        </a>
    </h2>
    {{ component('RoleTable') }}
{% endblock %}
