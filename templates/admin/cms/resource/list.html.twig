{% extends '@Forumify/admin/base.html.twig' %}
{% block title_page %}
    {{ 'admin.cms.resources.title'|trans }}
{% endblock %}
{% block body %}
    <h2 class="mb-4">
        {{ 'admin.cms.resources.title'|trans }}
        <a class="btn-primary btn-small btn-icon" href="{{ path('forumify_admin_cms_resource_create') }}">
            <i class="ph ph-plus"></i>
        </a>
    </h2>
    {{ component('ResourceTable') }}
{% endblock %}
