{% extends '@Forumify/admin/base.html.twig' %}
{% block body %}
    {% include '@Forumify/admin/components/confirm.html.twig' with {
        warningText: 'forum_delete_warning'|trans({ title: forum.title }),
        confirmPath: path('forumify_admin_forum_delete', { slug: forum.slug, confirmed: 1 }),
        cancelPath: path('forumify_admin_forum', { slug: (forum.parent ? forum.parent.slug : null) })
    } only %}
{% endblock %}
