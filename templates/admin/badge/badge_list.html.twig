{% extends '@Forumify/admin/base.html.twig' %}
{% block title_page %}
    {{ 'admin.badges.badges'|trans }}
{% endblock %}
{% block body %}
    <h2 class="mb-4">
        {{ 'admin.badges.badges'|trans }}
        <a class="btn-primary btn-small btn-icon" href="{{ path('forumify_admin_badge_create') }}">
            <i class="ph ph-plus"></i>
        </a>
    </h2>
    {{ component('BadgeTable') }}
{% endblock %}
