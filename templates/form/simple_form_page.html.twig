{% extends admin|default(false) ? '@Forumify/admin/base.html.twig' : '@Forumify/frontend/base.html.twig' %}
{% block body %}
    {% if title is defined %}
        {% set title = title|trans(titleArgs|default({})) %}
    {% endif %}
    {% include '@Forumify/admin/components/simple_form.html.twig' %}
{% endblock %}
