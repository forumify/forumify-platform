{% macro slot(widgets) %}
    {% for widget in widgets %}
        {% set template = widget|widget_template %}
        {% if template is not null %}
            {% set widgetContext = widget|widget_context %}
            {% include template with { widget: widgetContext } %}
        {% endif %}
    {% endfor %}
{% endmacro %}
