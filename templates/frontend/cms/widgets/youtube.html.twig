{% set videoId = widget.settings.videoId ?? null %}
{% if videoId is not null %}
    <div
        class="youtube-widget"
        {{ stimulus_controller('forumify/youtube', {
            videoId: videoId,
        }) }}>
        <div class="play-button"></div>
    </div>
{% endif %}
