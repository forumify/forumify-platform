{% extends '@Forumify/frontend/base.html.twig' %}
{% block title_page %}Inbox{% endblock %}
{% block body %}
    {% if initialThreadId is defined %}
        {{ component('MessageThreadList', { selectedThread: initialThreadId }) }}
    {% else %}
        {{ component('MessageThreadList') }}
    {% endif %}
{% endblock %}
