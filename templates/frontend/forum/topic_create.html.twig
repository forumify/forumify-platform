{% extends '@Forumify/frontend/base.html.twig' %}
{% block title_page %}
    {{ 'create_topic'|trans }}
{% endblock %}
{% block body %}
    <h1>{{ forum.title }} - {{ 'create_topic'|trans }}</h1>
    <div class="box">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="flex justify-center items-center gap-2">
            <button class="btn-primary" type="submit">{{ 'post'|trans }}</button>
            <a href="{{ path('forumify_forum_forum', {slug: forum.slug}) }}" class="btn-outlined">{{ 'cancel'|trans }}</a>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
