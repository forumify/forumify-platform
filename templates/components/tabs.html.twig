<div {{ stimulus_controller('forumify/forumify-platform/tabs') }}>
    <div class="tablist" role="tablist" {{ stimulus_target('forumify/forumify-platform/tabs', 'tabs') }}>
        {% block tabs %}
        {% endblock %}
    </div>
    <div class="d-none" {{ stimulus_target('forumify/forumify-platform/tabs', 'tabPanels') }}>
        {% block tabpanels %}
        {% endblock %}
    </div>
</div>
