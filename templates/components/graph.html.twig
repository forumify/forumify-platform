<div
    class="w-100"
    style="height: {{ height|default(120) }}px"
    {{ stimulus_controller('forumify/forumify-platform/chart', {
        type: this.type,
        data: this.data
    }) }}
></div>
