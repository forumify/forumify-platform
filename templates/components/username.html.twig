{%- if user is not null -%}
    <a class="{{ className|default('') }}" href="{{ path('forumify_forum_profile', { username: user.username }) }}">{{ user.displayName|default(user.username) }}</a>
{%- else -%}
    <span>{{ 'deleted'|trans }}</span>
{%- endif -%}
